import Vue from 'vue'
import Vuex from 'vuex'
import services from '@/services/api'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
